# 项目意图

## 项目目的

本项目将一组教学素材生成可审计、可发布的数字教材。系统应当能够更换
学科、素材和输出格式后继续工作。焊接只是历史测试主题，不是产品契约的
一部分。

核心目标不是删除相似段落，而是在固定大纲下改善学生的学习路径：

- 知识按照易于理解的顺序出现；
- 后续章节可以使用前文知识，但不无意义地重新完整讲授；
- 当后续出现承担应用、扩展或有明确目的的回忆时，应保留该出现；
- 只有在同一知识点、同一教学目标、同一认知层级上重复完整教学且没有
  有意义增量时，才应压缩或删除重复内容。

## 必须保持不变的内容

原始 BookPlan 是产品输入，不是语义优化结果。

1. 原始 BookPlan 生成器决定章节、任务、节、顺序、标题和知识归属。
2. BookPlan 生成后立即保存深度快照、目录签名和语义指纹，并冻结。
3. 知识分析、写作、修复、导出和发布都只能在冻结大纲之上建立 overlay，
   不得新增、删除、重排、改名目录节点，也不得转移知识归属。
4. 一个大纲节点可以对应多个语义 occurrence，但这种拆分不能创建新的
   可见目录节。

因此，系统只改善章节内部教学连续性和全书知识复用，不改变用户原有的
目录结构。

## 优化的语义单位

优化的核心对象是知识轨迹，而不是段落。

系统识别可学习、可迁移的概念、原理、方法、程序和技能；谨慎地合并别名
或拆分复合知识；并跟踪同一知识在全书中的每次出现。每次 occurrence 都
有明确的教学职责，例如：

- 建立一个概念或直觉；
- 在当前认知层级正式讲授；
- 为当前任务恢复完成任务所需的最小前置知识；
- 在新的任务或案例中应用已有知识；
- 增加新的条件、变体、限制或更高层理解。

同一知识点多次出现是正常情况。真正需要避免的不是多次出现，而是没有
教学增量的同目标完整重讲。

## 证据约束的生成

领域事实以授权的源素材为依据。LLM 可以提出语义解释，但模型输出本身不
是权威事实。每个计划中的教学贡献和正文 claim 都必须能够追溯到当前
occurrence 授权的证据，以及在允许情况下的前文 verified occurrence 或
固定任务/大纲上下文。

当计划超出当前授权证据时，系统必须收缩到证据真正支持的目标，或将该
occurrence 显式转为待审核/拒绝状态。禁止扩大证据范围、静默引入外部
知识，或降低证据门槛来让流程通过。

## 生成与验证流程

```text
原始素材 + 原始 BookPlan
    → 知识地图与 occurrence overlay
    → 证据约束的语义计划
    → 按教学职责生成 WritingBrief
    → Markdown 与 DigitalBook 渲染
    → conformance 与证据校验
    → 在允许范围内执行可审计修复
    → publication quality 检查与最终门禁
```

Markdown 和 DigitalBook 是同一语义 occurrence 的两种渲染，必须共享同一
role、brief、证据边界和稳定 occurrence identity。两个格式对同一 occurrence
作出不同教学职责判断属于缺陷。

验证应模拟学生沿章节顺序学习，而不是要求每章完全独立闭合。系统需要
检查：前文是否已经提供当前所需知识；是否需要按策略恢复上下文；重复
出现是否具有可解释的教学增量。recall policy 表示教材设计策略，不代表
系统已经观测到真实学习者遗忘。

## 质量与审计要求

所有关键决策都必须可人工检查：source mapping、occurrence 顺序、role、
所需与已提供 facet、证据 ID、conformance 结果、修复记录和最终状态。

UNSUPPORTED claim 仍是 blocker；PARTIALLY_SUPPORTED 必须单独报告，不能
静默转为 supported。publication quality PASS 不能替代语义证据验证。

只有语义验证、双端渲染对齐、publication quality 和审计完整性全部通过，
最终产物才可发布。失败时必须报告上游根因，不得静默回退到段落级删除或
未记录的旧路径。

## 通用性与评估

实现必须适用于任意学科，不得依赖焊接专名、焊接规则或测试夹具中的固定
标题。真实教材运行和构造的 trajectory fixture 必须分离，fixture 不得
进入生产产物。

baseline 与 semantic A/B 对比必须使用同一份冻结 BookPlan。字数减少或
教材变短本身不能证明去重成功；应重点比较重复完整教学、教学职责、章节
内连续性和学生学习路径。

